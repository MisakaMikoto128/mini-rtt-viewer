# Changelog

## 0.4.2 (2026-09-25)

关于框 + 远程访问 + 代码语言质量校验:

- **关于框(FR-27)**:底栏版本号即按钮,原生 `<dialog>` 弹关于信息
  (应用名/版本/一句话定位/MIT License/GitHub 仓库与更新日志外链);
  壳内外链经 /api/open-browser 打开(服务端仅放行本仓库 URL 前缀)
- **远程访问(FR-28)**:底栏「远程」按钮弹地址框——本机/局域网 URL 卡片,
  整行点击复制(成功短暂「✓ 已复制」);`--listen <ip>` 配置监听地址
  (默认 127.0.0.1 仅本机;0.0.0.0 开放局域网/互联网——管理台无鉴权,
  SECURITY.md 已如实说明);/api/addr 以 UDP connect 探测出网网卡
- **代码语言质量校验**:38 处注释/文档漂移修正(README 仍称「Rust + Slint」、
  SECURITY.md「不监听任何网络端口」、send_ending 注释与实际行为矛盾、
  JLinkARM.dll→JLink_x64.dll 统一等),术语权威表与 8 项重命名提案记档
- **修复**:首屏连接态失真(刷新后 ~2.2s 红点/「未连接」与真实连接态矛盾)、
  toast 遮挡发送条(重叠 19px→移至上方 8px)、右键「复制选中文本」在
  Chromium 下因 mousedown 塌缩选区而复制落空、`--port 0` 绕过端口校验
- **观感**:侧栏滚动条居中于右留白带(卡片零位移,未溢出时完全不可见)、
  远程弹窗地址卡片化、键盘焦点与四主题令牌跟随

## 0.4.1 (2026-09-24)

关闭假死修复 + 排版对齐 VS Code + 轮子替换现成方案 + 功能九项:

- **修复关闭进程僵尸假死**:tao 对退出标志的观察依赖消息流,且 ExitProcess 的
  DLL detach 阶段与存活的 WebView2 子进程做同步清理死等(现场:1 线程、端口
  残留、TerminateProcess 杀不死)。改为统一退出汇点——托盘显式移除 → 窗口线程
  drop webview → 3s 看门狗 TerminateProcess 兜底;开关 15+ 轮全部 <200ms 干净退出
- **日志排版对齐 VS Code**:默认字号 14px、行高 = round(字号 × 1.5)、字距
  -0.4px(A± 改字号联动重算行高);同屏行数 +30%
- **自造轮子替换现成方案**:WS 消息手拼 JSON → serde derive(线格式逐字节单测
  锁契约)、右键菜单 → 原生 popover、删剪贴板 execCommand 死回退、APPDATA 手解析
  → dirs::config_dir()
- **新功能(对照 serialhub 差距审计 S 档)**:底栏 RX/TX 速率、会话时长、
  「n / 500 行」上限语境、版本号显示;拖选/右键复制与导出成功的反馈 toast;
  日志时间戳开关([HH:MM:SS.mmm],搜索/导出不含前缀);壳内「在浏览器打开」按钮
- **修复**:右键「复制选中文本」在 Chromium 下因 mousedown 塌缩选区而复制落空
  (菜单打开瞬间快照);连接设置持久化断裂(此前改值 reload 全丢);连接/断开/
  重置/电源按钮在 demo 下无效(demo 升级虚拟 worker,手动断开不再自动重连,
  断开统一清统计);状态栏首连期文案不同步;自动滚动无法停留中部(follow-suspend);
  自动断帧开关无效;Shift+Enter 误发送;发送行尾不附加字节;HEX 容忍 0x 前缀/
  非法输入可见报错/开关双向转换

## 0.4.0 (2026-09-19)

Web 管理台复刻 Slint 版观感 + 团队闭环修复批(UX 像素审计 + QA 16 项黑盒):

- textarea 纳入四主题体系(此前恒白底)、发送组迁入发送条紧凑行、设备信息
  默认折叠(左面板 1130→765 满一屏)、控件高度统一 40、右缘对齐线三线合一、
  圆角收敛 {4,6,8}、小字对比度全主题达标
- 主题插件化(themes/*.css 拖入即用、运行中热载)、发送历史持久化、设置全量落盘
- HEX 发送 TX 计数口径修复(按解析后字节数)

## 0.3.0 (2026-09-16)

桌面壳架构迁移(SerialHub gui.rs 蓝本):tao 窗口 + wry WebView 内嵌管理台 +
托盘图标,Slint 退役;单 exe 双击即用,发布契约同步为单 exe 包。

## 0.2.1 (2026-09-15)

反馈修复批(QA/UX 验收报告观察项全清):

- rtt-web favicon 编译期内嵌(不再依赖工作目录,任意目录启动均正常)
- 新增 --help(用法/参数表,exit 0);未知参数 stderr 警告但向后兼容
- 搜索 debounce 窗口计数位立即置「—」,不再闪现上一轮陈旧计数
- demo 数据 ERR 行改前景红(背景 SGR 支持记 backlog)
- 前端行数口径与服务端 MAX_LOG_ROWS 对齐(500)

## 0.2.0 (2026-09-15)

浏览器管理台 + 四主题 + 应用图标 + 工程标准化:

- **浏览器管理台(rtt-web)**:独立二进制起本机服务,浏览器即用——WS 实时日志流、
  真机连接(与桌面版同一 worker 协议)、四主题、发送/暂停/清空/标记、目标设备
  可编辑下拉(输入即筛选);Playwright 黑盒测试入 CI
- **四套主题**:深色/浅色/OLED 纯黑/护眼暖色,下拉即时切换;旧 dark_theme 偏好自动迁移
- **应用图标**:窗口标题栏/任务栏/exe 资源(心跳波形 + 日志行)
- **工程标准化**(SerialHub 模板):CI 三道门禁(fmt/clippy/test)+ Playwright job、
  需求规格 FR-1..26、团队文档(charter/backlog/ADR-1..11)、GitHub Pages 官网
- **发布产物契约**:mini-rtt-viewer-<版本>-windows-x86_64.zip 双 exe(桌面 + rtt-web)
  含 README/LICENSE
- 修复:连接即崩(RefCell 重入)/手动滚到底裁末行/复制对不齐/主题下拉溢出/
  状态栏粘连/定时发送崩溃/清空竞态等(详见 git log 0.1.9..0.2.0)

## 0.1.9 (2026-08-31)

体验大版本:配置持久化 + 主题 + 搜索 + 换行 + 自由复制:

- **偏好持久化**:左侧面板全部配置(目标设备/J-Link 序列号/接口/速度/通道/行尾/断帧/自动滚动/HEX/字符集/字号/主题/发送历史/窗口几何/常亮/定时发送)落 `%APPDATA%/MiniRttViewer/prefs.json`,启动恢复、退出补写;J-Link 按序列号恢复(抗插拔顺序漂移)
- **字符集选择**:UTF-8/GBK/UTF-16 LE/Latin-1/ASCII,连接中切换动态生效(encoding_rs 增量解码)
- **深色/浅色主题**:左面板开关,全 UI 一键切换并记忆;浅色下控件灰边框压平(样式单文件覆盖)
- **正则搜索**:Ctrl+F 浮动条,计数 N/M + 上/下跳转 + 命中行高亮(regex-lite,体积仅 +0.13MB)
- **日志按宽度换行**:窗口窄于行宽时按显示列硬换行(CJK 2 列),拖拽窗口全量重排;小屏不再右裁
- **自由复制**:行+列级拖选(任意起点终点),拖选松手即复制、右键菜单(复制/全选)、Ctrl+C 三条路径;粘贴不再多换行
- **体验四件套**:发送历史(↑↓ 翻,去重记忆)/ 窗口几何记忆 / 屏幕常亮 / 定时发送(复用完整发送管线)
- **HEX 接收**:原始字节十六进制显示,动态生效;发送配置移入左面板(接收/发送分区)
- 修复:连接成功瞬间崩溃(tick 借用重入)、左面板滚动(Flickable→手动平移)、流数据下选中被即时清空
- 构建目录纪律:多 target 并存(累计 ~45G)清理,统一默认 target
- 测试 22→46,clippy 零警告

## 0.1.8 (2026-08-30)

对齐参考工程的八项轻量功能 + 面板滚动 + 连接崩溃修复:

- **会话标记**:「标记」按钮插入带时间戳的染色行,连接/断开自动插入;标记行与数据行同队列上屏、同上限裁剪,且绕过 ANSI 解析不污染设备流颜色状态
- **发送回显**:发送成功后日志区插灰色「» 原文」行
- **HEX 发送模式**:输入 `41 42 43` / `0x4142` 按原始字节发送(容忍空格/冒号/连字符分隔,格式错误状态栏提示)
- **全局快捷键**:F2 连接 / F3 断开(带状态守卫,未连接按 F3 不会卡"断开中")
- **日志字号 A-/A+**:行高探针与滚动网格随字号自动适应
- **统计栏**:TX/RX 字节 + 会话时长(连接清零,断开冻结,500ms 节流)
- **保存日志**:导出当前显示为 .log(原生文件对话框)
- **重置目标**:复位并运行(JLINKARM_Reset + Go),复位后自动重挂 RTT 继续接收
- **左侧面板整体可滚**:窗口矮于面板内容时可用滚轮/滑条滚动;右缘滑条可拖动,内容不足一屏时自动隐藏、布局不变
- **修复:连接成功瞬间崩溃**:tick 持日志泵借用期间插入标记导致 RefCell 二次借用,release 下直接 abort——表现为"连上就死/取不到数据";demo 模式现在模拟连接/断开循环,冒烟覆盖连接分支
- 测试 22→29,clippy 零警告

## 0.1.7 (2026-08-30)

修复 v0.1.6 引入的自动滚动失效:

- **修复:勾选自动滚动后日志也不跟随**。v0.1.6 把彩色日志改到 ListView 时用绑定驱动视口位置(`viewport-y: -offset`),而 Flickable 在首帧布局时必然自行赋值该属性——Slint 里赋值会永久破坏绑定,整个视口冻结:滚轮滚动与自动跟随(含 checkbox 勾选跳底部)全部失效
- 回到验证过的「行容器整体平移」滚动模式,逐行逐段着色保留;行数上限 1000→500(弃用虚拟化渲染后控制长日志的绘制成本)
- 补 UI 侧行模型裁剪同步:此前只有数据泵侧裁剪,UI 行模型只增不减,长时间运行无限增长

## 0.1.6 (2026-08-30)

对齐原 PySide6 工程核心能力 + ANSI 彩色日志的一轮大更新:

- **ANSI 转义颜色渲染**:设备输出的颜色转义序列(16/256/RGB 真彩色前景色)按段着色显示;解析用 vte(Alacritty/WezTerm 同款状态机),颜色状态跨行、跨读块正确保持;日志区改 ListView 虚拟化渲染,长日志只画可见行
- **J-Link 选择框**:多台调试器接入时下拉选择连接哪台;启动后台枚举 + 每次连接时自动刷新
- **修复"选了 B 连的是 A"**:无参 Open 在多台时仍会弹 probe 选择窗并用窗内选择覆盖程序选定(V8.24 实测);改为 SelectByUSBSN + SetHostIF + OpenEx 三件套,彻底不弹窗、选定必生效;打开后校验实际序列号,不符告警;连接状态栏追加 SN 直观可见
- **目标设备选择升级**:单控件——输入即筛选(约 1.2 万设备型号),点右侧箭头弹原生候选列表;设备库启动后台枚举并缓存到磁盘,二次启动零加载
- **设备信息面板**:连接后显示固件版本、硬件版本、序列号、核心名称、核心 ID、CPU 类型、目标设备、接口、速度(可折叠)
- **电源输出开关**:J-Link 19 脚给目标供电,连接后可用
- **自动滚动勾选框**:与日志跟随状态双向同步——上滚自动取消勾选,重新勾选跳回底部
- **修复滚动根因 bug**:Slint 长度除法语义导致滚动量被缩小行高倍——上滚瞬移、快速下滚弹回顶部、亚行距"微小抖动"同源,一并修复;每格滚轮稳定走整数行
- UI 细节:目标设备与候选合并为单控件、卡片标题去装饰线、修复折叠箭头字形乱码(Segoe UI 无 ▸ 字形)、断帧说明文字换行防裁切

## 0.1.5 (2026-08-30)

滚动体验与实时性的一轮重做:

- **滚动行对齐**:日志区改为自建滚动容器,滚轮位移按整数行量化对齐——连续上翻时上一行精确落进上一屏的位置,行位置稳定不抖;滚离底部即停止自动跟随,滚回底部自动恢复
- **实时性**:界面刷新周期 200ms→10ms。此前均匀发送的消息会被攒成一批同时冒出(视觉上"两条出来一次");现在逐条均匀上屏,微秒级时间戳实测与设备发送节奏一致
- **自动断帧真正可用**:判定从 200ms 的界面轮询移入 worker 读循环(真实时间戳、5ms 精度),1~200ms 任意设定值都真实生效;界面上修改立即应用,无需重连
- **断行方式可选**:关闭 / 按数据块(设备一次发送一行)/ 时间窗(按周期聚合一行为一行);**接收行尾与发送行尾独立设置**
- **连接按钮不再闪烁**:连接过程进度与最终结果分离上报,按钮从按下到出结果只切换一次;连接失败在 worker 内自动重试 3 次
- 修复日志文本块随内容更新左右晃动(水平视口被内容宽度反馈牵动)
- 修复贴底时整个日志滚出视野导致日志区空白
- 发送区去掉多余外框;断帧间隔输入框加宽不再裁字
- 新增 `--demo-log` 启动参数:无设备演示/测试数据源(中英混排 + emoji)

## 0.1.4 (2026-08-29)

- **自动断行改为固定时间窗**:此前实现是"静默超时才切行",密集数据流下几乎不触发(只能靠 256 字符兜底,看起来像没实现)。现在无论数据到达节奏如何,每超过设定周期(默认 100ms,可调)强制出一行,行长短均匀
- **连接按钮不再闪烁**:连接/取消连接/断开合并为位置固定的单按钮,只切换文字;此前是两个按钮互斥显隐,切换时布局重排产生闪烁
- **新增"暂停接收"**:暂停期间新数据直接丢弃(不进日志、不占缓冲),恢复后继续;按钮在"暂停接收/继续接收"间切换

## 0.1.3 (2026-08-29)

交互重做,修掉"按钮要点两下"和复制体验:

- **按钮单击直触发**:全部控件换成 Slint 标准按钮(自定义 TouchArea 按钮存在点击丢失/焦点怪癖),蓝色主按钮样式保留
- **日志可直接选中复制**:日志区改为可选中文本,鼠标拖选后 Ctrl+C 复制(选中高亮为强调色);删除"复制全部"按钮及剪贴板依赖
- "自动断帧"更名"自动断行",行为不变:无换行符的裸流按静默超时(默认 100ms,可调)切行,另加单行 256 字符硬上限
- 日志文本上限 10 万字符,超出丢最旧(只读文本全量渲染的防卡措施)

## 0.1.2 (2026-08-29)

对抗性测试驱动的一轮彻底加固:

- **连接卡死的真正根因修复**:J-Link DLL 在连接出错时会弹**隐藏的模态对话框**等待确认,worker 看起来永远"连接中"。现在 open 后立即抑制全部 DLL 弹窗(pylink 同款命令序列 + SetBatchMode),无效设备等错误改为快速返回错误码(实测 12 秒内报 错误码 -273)
- **关窗不再留僵尸进程**:应用退出信号 + worker 存活句柄,关窗(含连接中)后 3 秒内干净退出并释放 J-Link;worker 卡死时强制退出兜底
- **单实例互斥**:第二个实例弹窗提示并退出,不再出现两进程同时连一个 J-Link
- **焦点链修复**:RTT 通道从 SpinBox 改为 ComboBox(SpinBox 内部会困住 Tab);移除左侧面板 ScrollView(其内部焦点链与外层断开);全部按钮暴露为标准 UIA 按钮(读屏器/自动化可按压),触发改为按下即生效 + UIA Invoke 双路去重
- **布局防护**:窗口增加 min/max 尺寸限制
- 芯片型号自动去首尾空白;空型号拒绝连接并提示
- 断帧超时输入框改普通文本(UIA 可读写),非法值由解析层兜底
- 清空日志后状态栏恢复完整连接信息

## 0.1.1 (2026-08-29)

修复首轮真机使用暴露的一批问题:

- **连接卡住无出路**:连接中现在显示"取消连接"按钮;断开改为"断开中…"状态,等 worker 真正退出(Exited 门闩)后才允许再次连接——根治两个 worker 并发抢 J-Link 导致的数据损坏/状态错乱
- **超长行横向溢出**:断帧在静默超时之外增加单行 256 字符硬上限,积压数据批量到达不再切出一整屏单行;行内容自动换行,高度自适应
- **文字重叠**:日志行委托改为不透明背景,盖住 GPU 局部重绘残影;保留 software 渲染器,显示异常时可设 `SLINT_BACKEND=winit-software` 兜底
- **按钮要点两次**:按钮改为按下即触发,不依赖释放位置
- **无法复制日志**:新增"复制全部"按钮(写入系统剪贴板)
- **release 版带 cmd 黑框**:release 构建隐藏控制台窗口
- 连接失败提示增加"尝试降低速率"指引(Cortex-M0 高速率下 SWD 可能不稳)

## 0.1.0 (2026-08-29)

首个公开版本。

- J-Link RTT 连接(芯片型号 / SWD / JTAG / 速率 / 通道)
- UTF-8 / emoji 完整解码,跨块多字节序列拼接,坏字节降级为替换符
- ANSI 转义剥离
- 行式虚拟化日志渲染,高吞吐不卡 UI(实测 <1% 单核)
- 自动断帧(静默超时可调,应对无换行符裸流)
- 自动滚动(用户上翻时不拉回底部)
- 数据发送(Enter 发送,自动追加 \r\n)
- 暗色主题,键盘完整可操作
