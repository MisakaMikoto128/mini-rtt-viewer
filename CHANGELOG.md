# Changelog

## 0.1.8 (2026-08-30)

对齐参考工程的八项轻量功能 + 面板滚动 + 连接崩溃修复:

- **会话标记**:「标记」按钮插入带时间戳的染色行,连接/断开自动插入;标记行与数据行同队列上屏、同上限裁剪,且绕过 ANSI 解析不污染设备流颜色状态
- **发送回显**:发送成功后日志区插灰色「» 原文」行
- **HEX 发送模式**:输入 `41 42 43` / `0x4142` 按原始字节发送(容忍空格/冒号/连字符分隔,格式错误状态栏提示)
- **全局快捷键**:F2 连接 / F3 断开(带状态守卫,未连接按 F3 不会卡"断开中")
- **日志字号 A-/A+**:行高探针与滚动网格随字号自动适应
- **统计栏**:TX/RX 字节 + 会话时长(连接清零,断开冻结,500ms 节流)
- **保存日志**:导出当前显示为 .log(原生文件对话框)
- **重置目标**:复位并运行(JLINKARM_Reset + Go),复位后自动重挂 RTT 继续接收
- **左侧面板整体可滚**:窗口矮于面板内容时可用滚轮/滑条滚动;右缘滑条可拖动,内容不足一屏时自动隐藏、布局不变
- **修复:连接成功瞬间崩溃**:tick 持日志泵借用期间插入标记导致 RefCell 二次借用,release 下直接 abort——表现为"连上就死/取不到数据";demo 模式现在模拟连接/断开循环,冒烟覆盖连接分支
- 测试 22→29,clippy 零警告

## 0.1.7 (2026-08-30)

修复 v0.1.6 引入的自动滚动失效:

- **修复:勾选自动滚动后日志也不跟随**。v0.1.6 把彩色日志改到 ListView 时用绑定驱动视口位置(`viewport-y: -offset`),而 Flickable 在首帧布局时必然自行赋值该属性——Slint 里赋值会永久破坏绑定,整个视口冻结:滚轮滚动与自动跟随(含 checkbox 勾选跳底部)全部失效
- 回到验证过的「行容器整体平移」滚动模式,逐行逐段着色保留;行数上限 1000→500(弃用虚拟化渲染后控制长日志的绘制成本)
- 补 UI 侧行模型裁剪同步:此前只有数据泵侧裁剪,UI 行模型只增不减,长时间运行无限增长

## 0.1.6 (2026-08-30)

对齐原 PySide6 工程核心能力 + ANSI 彩色日志的一轮大更新:

- **ANSI 转义颜色渲染**:设备输出的颜色转义序列(16/256/RGB 真彩色前景色)按段着色显示;解析用 vte(Alacritty/WezTerm 同款状态机),颜色状态跨行、跨读块正确保持;日志区改 ListView 虚拟化渲染,长日志只画可见行
- **J-Link 选择框**:多台调试器接入时下拉选择连接哪台;启动后台枚举 + 每次连接时自动刷新
- **修复"选了 B 连的是 A"**:无参 Open 在多台时仍会弹 probe 选择窗并用窗内选择覆盖程序选定(V8.24 实测);改为 SelectByUSBSN + SetHostIF + OpenEx 三件套,彻底不弹窗、选定必生效;打开后校验实际序列号,不符告警;连接状态栏追加 SN 直观可见
- **目标设备选择升级**:单控件——输入即筛选(约 1.2 万设备型号),点右侧箭头弹原生候选列表;设备库启动后台枚举并缓存到磁盘,二次启动零加载
- **设备信息面板**:连接后显示固件版本、硬件版本、序列号、核心名称、核心 ID、CPU 类型、目标设备、接口、速度(可折叠)
- **电源输出开关**:J-Link 19 脚给目标供电,连接后可用
- **自动滚动勾选框**:与日志跟随状态双向同步——上滚自动取消勾选,重新勾选跳回底部
- **修复滚动根因 bug**:Slint 长度除法语义导致滚动量被缩小行高倍——上滚瞬移、快速下滚弹回顶部、亚行距"微小抖动"同源,一并修复;每格滚轮稳定走整数行
- UI 细节:目标设备与候选合并为单控件、卡片标题去装饰线、修复折叠箭头字形乱码(Segoe UI 无 ▸ 字形)、断帧说明文字换行防裁切

## 0.1.5 (2026-08-30)

滚动体验与实时性的一轮重做:

- **滚动行对齐**:日志区改为自建滚动容器,滚轮位移按整数行量化对齐——连续上翻时上一行精确落进上一屏的位置,行位置稳定不抖;滚离底部即停止自动跟随,滚回底部自动恢复
- **实时性**:界面刷新周期 200ms→10ms。此前均匀发送的消息会被攒成一批同时冒出(视觉上"两条出来一次");现在逐条均匀上屏,微秒级时间戳实测与设备发送节奏一致
- **自动断帧真正可用**:判定从 200ms 的界面轮询移入 worker 读循环(真实时间戳、5ms 精度),1~200ms 任意设定值都真实生效;界面上修改立即应用,无需重连
- **断行方式可选**:关闭 / 按数据块(设备一次发送一行)/ 时间窗(按周期聚合一行为一行);**接收行尾与发送行尾独立设置**
- **连接按钮不再闪烁**:连接过程进度与最终结果分离上报,按钮从按下到出结果只切换一次;连接失败在 worker 内自动重试 3 次
- 修复日志文本块随内容更新左右晃动(水平视口被内容宽度反馈牵动)
- 修复贴底时整个日志滚出视野导致日志区空白
- 发送区去掉多余外框;断帧间隔输入框加宽不再裁字
- 新增 `--demo-log` 启动参数:无设备演示/测试数据源(中英混排 + emoji)

## 0.1.4 (2026-08-29)

- **自动断行改为固定时间窗**:此前实现是"静默超时才切行",密集数据流下几乎不触发(只能靠 256 字符兜底,看起来像没实现)。现在无论数据到达节奏如何,每超过设定周期(默认 100ms,可调)强制出一行,行长短均匀
- **连接按钮不再闪烁**:连接/取消连接/断开合并为位置固定的单按钮,只切换文字;此前是两个按钮互斥显隐,切换时布局重排产生闪烁
- **新增"暂停接收"**:暂停期间新数据直接丢弃(不进日志、不占缓冲),恢复后继续;按钮在"暂停接收/继续接收"间切换

## 0.1.3 (2026-08-29)

交互重做,修掉"按钮要点两下"和复制体验:

- **按钮单击直触发**:全部控件换成 Slint 标准按钮(自定义 TouchArea 按钮存在点击丢失/焦点怪癖),蓝色主按钮样式保留
- **日志可直接选中复制**:日志区改为可选中文本,鼠标拖选后 Ctrl+C 复制(选中高亮为强调色);删除"复制全部"按钮及剪贴板依赖
- "自动断帧"更名"自动断行",行为不变:无换行符的裸流按静默超时(默认 100ms,可调)切行,另加单行 256 字符硬上限
- 日志文本上限 10 万字符,超出丢最旧(只读文本全量渲染的防卡措施)

## 0.1.2 (2026-08-29)

对抗性测试驱动的一轮彻底加固:

- **连接卡死的真正根因修复**:J-Link DLL 在连接出错时会弹**隐藏的模态对话框**等待确认,worker 看起来永远"连接中"。现在 open 后立即抑制全部 DLL 弹窗(pylink 同款命令序列 + SetBatchMode),无效设备等错误改为快速返回错误码(实测 12 秒内报 错误码 -273)
- **关窗不再留僵尸进程**:应用退出信号 + worker 存活句柄,关窗(含连接中)后 3 秒内干净退出并释放 J-Link;worker 卡死时强制退出兜底
- **单实例互斥**:第二个实例弹窗提示并退出,不再出现两进程同时连一个 J-Link
- **焦点链修复**:RTT 通道从 SpinBox 改为 ComboBox(SpinBox 内部会困住 Tab);移除左侧面板 ScrollView(其内部焦点链与外层断开);全部按钮暴露为标准 UIA 按钮(读屏器/自动化可按压),触发改为按下即生效 + UIA Invoke 双路去重
- **布局防护**:窗口增加 min/max 尺寸限制
- 芯片型号自动去首尾空白;空型号拒绝连接并提示
- 断帧超时输入框改普通文本(UIA 可读写),非法值由解析层兜底
- 清空日志后状态栏恢复完整连接信息

## 0.1.1 (2026-08-29)

修复首轮真机使用暴露的一批问题:

- **连接卡住无出路**:连接中现在显示"取消连接"按钮;断开改为"断开中…"状态,等 worker 真正退出(Exited 门闩)后才允许再次连接——根治两个 worker 并发抢 J-Link 导致的数据损坏/状态错乱
- **超长行横向溢出**:断帧在静默超时之外增加单行 256 字符硬上限,积压数据批量到达不再切出一整屏单行;行内容自动换行,高度自适应
- **文字重叠**:日志行委托改为不透明背景,盖住 GPU 局部重绘残影;保留 software 渲染器,显示异常时可设 `SLINT_BACKEND=winit-software` 兜底
- **按钮要点两次**:按钮改为按下即触发,不依赖释放位置
- **无法复制日志**:新增"复制全部"按钮(写入系统剪贴板)
- **release 版带 cmd 黑框**:release 构建隐藏控制台窗口
- 连接失败提示增加"尝试降低速率"指引(Cortex-M0 高速率下 SWD 可能不稳)

## 0.1.0 (2026-08-29)

首个公开版本。

- J-Link RTT 连接(芯片型号 / SWD / JTAG / 速率 / 通道)
- UTF-8 / emoji 完整解码,跨块多字节序列拼接,坏字节降级为替换符
- ANSI 转义剥离
- 行式虚拟化日志渲染,高吞吐不卡 UI(实测 <1% 单核)
- 自动断帧(静默超时可调,应对无换行符裸流)
- 自动滚动(用户上翻时不拉回底部)
- 数据发送(Enter 发送,自动追加 \r\n)
- 暗色主题,键盘完整可操作
